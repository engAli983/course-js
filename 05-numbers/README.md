# 📘 الدرس 05: الأرقام (Numbers)

## 📌 خصائص الأرقام في JavaScript

### Syntactic Sugar
```javascript
console.log(1000000);    // صعب القراءة
console.log(1_000_000);  // نفس الرقم بس أسهل في القراءة ✅
console.log(1e6);        // 1 وبعدها 6 أصفار = 1000000
```

### حدود الأرقام
```javascript
Number.MAX_SAFE_INTEGER   // أكبر رقم آمن
Number.MIN_SAFE_INTEGER   // أصغر رقم آمن
Number.MAX_VALUE          // أكبر قيمة ممكنة
Number.MIN_VALUE          // أصغر قيمة ممكنة
```

## 🔹 Number Methods

| الدالة | الوظيفة | مثال |
|--------|---------|------|
| `toString()` | تحويل الرقم لنص | `(100).toString()` → `"100"` |
| `toFixed(n)` | تحديد عدد الأرقام العشرية | `(100.555).toFixed(2)` → `"100.56"` |
| `parseInt()` | تحويل النص لرقم صحيح | `parseInt("100 Ali")` → `100` |
| `parseFloat()` | تحويل النص لرقم عشري | `parseFloat("100.55")` → `100.55` |
| `isInteger()` | هل هو عدد صحيح؟ | `Number.isInteger(100)` → `true` |
| `isNaN()` | هل هو NaN؟ | `Number.isNaN("Ali")` → `false` |

> 💡 **الفرق:** `parseInt("100 Ali")` → `100` لكن `Number("100 Ali")` → `NaN`
> `parseInt` بيعرف يستخرج الرقم من النص، لكن `Number` لا.

---

📺 [شاهد الدرس على يوتيوب](https://www.youtube.com/playlist?list=PLDoPjvoNmBAx3kiplQR_oeDqLDBUDYwVv) | [🔙 العودة للفهرس](../README.md)
