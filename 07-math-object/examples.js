// =====================================================
// 📘 الدرس 07: كائن الرياضيات (Math Object)
// =====================================================

// --- round() ---
console.log(Math.round(99.2));   // 99 - تقريب لأقرب
console.log(Math.round(99.5));   // 100

// --- ceil() ---
console.log(Math.ceil(99.2));    // 100 - تقريب لأعلى دايماً

// --- floor() ---
console.log(Math.floor(99.9));   // 99 - تقريب لأسفل دايماً

// --- max() و min() ---
console.log(Math.max(10, 20, 100, -100)); // 100 - أكبر رقم
console.log(Math.min(10, 20, 100, -100)); // -100 - أصغر رقم

// --- pow() ---
console.log(Math.pow(2, 4));     // 16 - 2 أُس 4
console.log(2 ** 4);             // 16 - نفس الفكرة

// --- random() ---
console.log(Math.random());      // رقم عشوائي بين 0 و 1

// رقم عشوائي من 1 لـ 10
console.log(Math.floor(Math.random() * 10) + 1);

// --- trunc() ---
console.log(Math.trunc(99.5));   // 99 - بيشيل الجزء العشري
