# 📘 الدرس 07: كائن الرياضيات (Math Object)

## 📌 Math Object Methods

| الدالة | الوظيفة | مثال | النتيجة |
|--------|---------|------|---------|
| `Math.round()` | تقريب | `Math.round(99.5)` | `100` |
| `Math.ceil()` | تقريب لأعلى دايماً | `Math.ceil(99.2)` | `100` |
| `Math.floor()` | تقريب لأسفل دايماً | `Math.floor(99.9)` | `99` |
| `Math.max()` | أكبر رقم | `Math.max(10, 100)` | `100` |
| `Math.min()` | أصغر رقم | `Math.min(10, 100)` | `10` |
| `Math.pow()` | رقم مرفوع لأُس | `Math.pow(2, 4)` | `16` |
| `Math.random()` | رقم عشوائي (0 - 1) | `Math.random()` | `0.xxx` |
| `Math.trunc()` | إزالة الجزء العشري | `Math.trunc(99.5)` | `99` |

## 🔹 أمثلة عملية

### التقريب
```javascript
Math.round(99.2);  // 99 (تقريب لأقرب)
Math.round(99.5);  // 100
Math.ceil(99.2);   // 100 (لأعلى دايماً)
Math.floor(99.9);  // 99 (لأسفل دايماً)
Math.trunc(99.5);  // 99 (بيشيل الكسور بس)
```

### رقم عشوائي بين قيمتين
```javascript
// رقم عشوائي من 1 لـ 10
let random = Math.floor(Math.random() * 10) + 1;
```

---

📺 [شاهد الدرس على يوتيوب](https://www.youtube.com/playlist?list=PLDoPjvoNmBAx3kiplQR_oeDqLDBUDYwVv) | [🔙 العودة للفهرس](../README.md)
