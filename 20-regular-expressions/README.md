# 📘 الدرس 20: التعبيرات النمطية (Regular Expressions)

## 📌 إنشاء RegEx
```javascript
let re = /pattern/flags;
```

## 🔹 Modifiers (الأعلام)

| Flag | الوظيفة |
|------|---------|
| `g` | بحث شامل (Global) |
| `i` | تجاهل حالة الحروف (Case Insensitive) |

```javascript
let str = "Hello Ali I Love ali";
console.log(str.match(/ali/gi)); // ["Ali", "ali"]
```

## 🔹 Ranges

```javascript
/[a-z]/g     // حروف صغيرة
/[A-Z]/g     // حروف كبيرة
/[0-9]/g     // أرقام
/[^0-9]/g    // ليس أرقام
/(info|org)/gi // أي واحدة من المجموعة
```

## 🔹 Character Classes

| الرمز | المعنى |
|-------|--------|
| `.` | أي حرف |
| `\w` | حرف أو رقم أو _ |
| `\d` | رقم |
| `\s` | مسافة |
| `\b` | حدود الكلمة |

## 🔹 Quantifiers

| الرمز | المعنى |
|-------|--------|
| `+` | واحد أو أكتر |
| `*` | صفر أو أكتر |
| `?` | صفر أو واحد |
| `{3}` | بالظبط 3 |
| `{3,5}` | من 3 لـ 5 |
| `{3,}` | 3 أو أكتر |

## 🔹 Anchors و Lookahead
```javascript
/^we/gi      // بيبدأ بـ "we"
/ing$/g      // بينتهي بـ "ing"
/\d\w{5}(?=z)/gi // 5 حروف متبوعة بـ z
```

## 🔹 Replace
```javascript
text.replace("@", "JS");     // أول واحد بس
text.replaceAll("@", "JS");  // كلهم
text.replace(/@/g, "JS");    // كلهم بـ RegEx
```

---

📺 [شاهد الدرس على يوتيوب](https://www.youtube.com/playlist?list=PLDoPjvoNmBAx3kiplQR_oeDqLDBUDYwVv) | [🔙 العودة للفهرس](../README.md)
