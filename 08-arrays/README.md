# 📘 الدرس 08: المصفوفات (Arrays)

## 📌 إيه هي المصفوفة؟

المصفوفة هي **قائمة مرتبة** بتحتوي على أكتر من قيمة.

```javascript
let friends = ["Ali", "Ahmed", "Sayed"];
console.log(friends[0]); // "Ali"
```

## 🔹 Array Methods

### إضافة وحذف عناصر

| الدالة | الوظيفة |
|--------|---------|
| `push()` | إضافة في الآخر |
| `unshift()` | إضافة في الأول |
| `pop()` | حذف من الآخر |
| `shift()` | حذف من الأول |

```javascript
let arr = ["A", "B"];
arr.push("C");      // ["A", "B", "C"]
arr.unshift("Z");   // ["Z", "A", "B", "C"]
arr.pop();          // ["Z", "A", "B"]
arr.shift();        // ["A", "B"]
```

### البحث

| الدالة | الوظيفة |
|--------|---------|
| `indexOf()` | أول index للعنصر |
| `lastIndexOf()` | آخر index للعنصر |
| `includes()` | هل العنصر موجود؟ |

### القص والتعديل

| الدالة | الوظيفة |
|--------|---------|
| `slice(start, end)` | بيرجع نسخة جديدة (مش بيعدل الأصل) |
| `splice(start, deleteCount, ...items)` | بيعدل المصفوفة الأصلية |

### الدمج والتحويل

| الدالة | الوظيفة |
|--------|---------|
| `concat()` | دمج مصفوفتين أو أكتر |
| `join(separator)` | تحويل المصفوفة لنص |
| `sort()` | ترتيب |
| `reverse()` | عكس الترتيب |

---

📺 [شاهد الدرس على يوتيوب](https://www.youtube.com/playlist?list=PLDoPjvoNmBAx3kiplQR_oeDqLDBUDYwVv) | [🔙 العودة للفهرس](../README.md)
